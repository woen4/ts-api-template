import type { AsyncMaybe } from "~/core/types/maybe";
import type { {{singularize (capitalize name)}} } from "~/domain/entities";
{{{brandImports brandedFields}}}
import type { Db } from "~/infra/database";
import type { ExtendedModel } from "~/infra/database/repositories/types";

type {{singularize (capitalize name)}}Collection = Db["orm"]["public"]["{{singularize (capitalize name)}}"];

export type {{singularize (capitalize name)}}Where = Parameters<{{singularize (capitalize name)}}Collection["where"]>[0];
export type {{singularize (capitalize name)}}Create = Parameters<{{singularize (capitalize name)}}Collection["create"]>[0];
export type {{singularize (capitalize name)}}Update = Parameters<
	ReturnType<{{singularize (capitalize name)}}Collection["where"]>["update"]
>[0];

export type I{{pluralize (capitalize name)}}Repository = {{pluralize (capitalize name)}}Repository

export class {{pluralize (capitalize name)}}Repository {
	static usedAs = "{{pluralize (lowercase name)}}Repository";

	constructor(private readonly db: Db) {}

	private get {{pluralize (lowercase name)}}() {
		return this.db.orm.public.{{singularize (capitalize name)}};
	}

	create(data: {{singularize (capitalize name)}}Create) {
		return this.{{pluralize (lowercase name)}}
			.create(data)
			.then(({{singularize (lowercase name)}}) => {{pluralize (capitalize name)}}Repository.toDomain({{singularize (lowercase name)}}));
	}

	update(params: { where: {{singularize (capitalize name)}}Where; data: {{singularize (capitalize name)}}Update }) {
		return this.{{pluralize (lowercase name)}}.where(params.where).update(params.data);
	}

	delete(where: {{singularize (capitalize name)}}Where) {
		return this.{{pluralize (lowercase name)}}.where(where).delete();
	}

	async findMany(where?: {{singularize (capitalize name)}}Where): Promise<{{singularize (capitalize name)}}[]> {
		const {{pluralize (lowercase name)}} = await (where ? this.{{pluralize (lowercase name)}}.where(where) : this.{{pluralize (lowercase name)}}).all();

		return {{pluralize (lowercase name)}}.map(({{singularize (lowercase name)}}) => {{pluralize (capitalize name)}}Repository.toDomain({{singularize (lowercase name)}}));
	}

	async findUnique(where: {{singularize (capitalize name)}}Where): AsyncMaybe<{{singularize (capitalize name)}}> {
		const {{singularize (lowercase name)}} = await this.{{pluralize (lowercase name)}}.where(where).first();

		if (!{{singularize (lowercase name)}}) return null;

		return {{pluralize (capitalize name)}}Repository.toDomain({{singularize (lowercase name)}})
	}

	static toDomain(record: ExtendedModel<"{{singularize (capitalize name)}}">): {{singularize (capitalize name)}} {
		return {
			...record,
{{{brandAssignments brandedFields "record"}}}
		};
	}
}
