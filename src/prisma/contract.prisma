// use prisma-next

model User {
  id   String @id @default(cuid(2))
  name String

  posts Post[]

  @@map("users")
}

model Post {
  id    String @id @default(cuid(2))
  title String

  userId String
  user   User   @relation(fields: [userId], references: [id])

  @@map("posts")
}
